# 图 1：板凳真实尺度与直线弦长约束

本图采用“圆域总览＋局部放大”结构：

- 圆域半径为 `8.80 m（880 cm）`；
- 龙头板凳实长 `3.41 m`，孔中心直线距离 `2.86 m`；
- 龙身板凳实长 `2.20 m`，孔中心直线距离 `1.65 m`；
- 局部图对比直线弦长与两孔间螺线弧长。

运行：

```powershell
python .\plot.py
```
