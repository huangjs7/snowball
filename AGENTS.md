# 仓库内 AI 协作规则

- 环境是 Windows，仓库根目录为 `C:\Users\ASUS\Desktop\数学建模`。
- 当前任务是数学建模竞赛 A 题“板凳龙”。开始前阅读根 README、负责目录的 README 和最新交接记录。
- 程序负责人在 `建模计算/问题N/` 内同时维护模型说明、代码和本问输出。没有实际文件时不建目录。
- 绘图负责人维护 `图片存放/`；论文负责人只维护 `论文/主稿.tex`。
- `赛题+资料/赛题/A题附件/` 是官方空白模板，不直接写入。
- 最终提交件放入按需创建的 `提交/`。文件进入 `提交/` 后，从原输出目录移走，避免两份同名结果。
- 图片按 `图片存放/问题N/图XX_名称/` 存放。Agent 新建或接手图片前阅读 `图片存放/README.md`。
- 代码使用相对仓库路径，不写死用户名或桌面路径。随机算法固定种子，数值结果注明单位和精度。
- 不删除、覆盖或移动其他负责人正在修改的文件。跨职责变更写入 `交接文档/`。
- 完成后运行相关检查，并报告修改文件、运行结果、输出位置和待确认事项。
